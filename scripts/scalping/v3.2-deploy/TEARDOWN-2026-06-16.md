# Infra teardown — 2026-06-16

Trimmed the VPS (srv1370094) to **only** the three live strategies Rich is
running. Everything else stopped, saved (tarball), and archived. Fully
reversible — nothing `rm -rf`'d.

## Now running (keepers)

| Strategy | Bridge | Dashboard | Dir | Route |
|---|---|---|---|---|
| **V3.2** (BloFin demo) | `scalping-v3.2` :8789 | `scalping-v3-2-dashboard` :8097 → v3-2.agentneo.cloud | `/docker/scalping-v3.2`, `/docker/scalping-v3-2-dashboard` | `scalping-v3-2-dash.yml` |
| **Scalper** (Lighter) | `scalper-bridge` | `scalper-dashboard` → scalper.agentneo.cloud | `/docker/scalper-paper` | `scalper-dash.yml` |
| **Rebound** (Lighter) | `rebound-bridge` | `rebound-dashboard` → rebound.agentneo.cloud | `/docker/boost-testnet` | `rebound-dash.yml` |

Plus infra kept: `traefik-mncm-traefik-1`, `/docker/lighter-dashboard` (shared
dashboard image that scalper + rebound dashboards build from), `lighter.htpasswd`.

## Retired (stopped + saved + archived)

- **Containers removed:** `scalping-v3.1`, `squeeze-bridge`, `squeeze-dashboard`.
- **Dirs archived** → `/docker/_archive/`: `scalping-v3`, `scalping-v3.1`,
  `squeeze-paper`, `lighter-paper`, `z-fade-paper`.
- **Traefik routes retired** → `/root/retired-routes/`: blofin-bridge, pro-v3,
  pro-v3-dash, squeeze-dash, boost-dash, hlsm-bridge, meme, meridian, predict,
  runner, lighter.

## Backups

- VPS: `/root/retired-2026-06-16/*.tar.gz` (per-dir) + `/root/scalping-v3.1-presave-2026-06-16.tar.gz`.
- Off-site (local): `Leverage/_local_backups/*.tar.gz` (git-ignored — they hold `.env` secrets).

## Restore (if ever needed)

```bash
# bring a retired project back, e.g. V3.1:
cd /docker/_archive && cp -r scalping-v3.1 /docker/ && cd /docker/scalping-v3.1
chmod 0777 data && docker compose up -d        # (V3.1 + V3.2 both trade ZEC — run one)
# restore a route: cp /root/retired-routes/<name>.yml /docker/traefik-mncm/config/
```

Note: V3.1 revert is now tarball/archive-based (not a hot container), since V3.2
is the committed path. The archived dir + DB are intact.
